version: "3"
services:
  api:
    build:
      dockerfile: /server/Dockerfile
    container_name: dev_api
    ports:
      - 3000:3000
    environment:
      - DB_NAME=scores
      - DB_CONNECTION=mongodb://gjam:quizpass@db:27017
    depends_on:
      - db
    command: bash -c "npm install && npm run start"
  db:
    container_name: dev_db
    volumes:
      - "dbdata:/var/lib/mongodb/data"
      - "./db/seeds.js:/docker-entrypoint-initdb.d/seeds.js:ro"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=HWMooo
      - MONGO_INITDB_DATABASE=scores
      - MONGO_INITDB_ROOT_PASSWORD=6dVDkX7H3KdzSEOD
volumes:
  dbdata:
networks:
  default:
    name: app_dev
